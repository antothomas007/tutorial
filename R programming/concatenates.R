#Write a function in R that takes two strings as input and concatenates them together, separated by a space.

a = readline(prompt = "enter the 1st string : ")
b = readline(prompt = "enter the 2nd string : ")
print(paste(a,b))